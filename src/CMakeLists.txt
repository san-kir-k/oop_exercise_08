cmake_minimum_required(VERSION 3.17)

project(oop_exercise_08)

set(SOURCE main.cpp dot.cpp octagon.cpp square.cpp triangle.cpp pubsub.cpp)
set(EXEC lr8)

add_definitions(-std=c++17 -pedantic -Wall -pthread)
include_directories(${CMAKE_CURRENT_SOURCE_DIR})
add_executable(${EXEC} ${SOURCE})
